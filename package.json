{
  "packageManager": "pnpm@7.28.0",
  "scripts": {
    "dev": "pnpm -F ./web dev",
    "build": "wasm-pack build lib --release --out-name=lib --scope tedbyron --target web && pnpm -F ./web build",
    "preview": "pnpm -F ./web preview",
    "lint": "pnpm -F ./web lint",
    "format": "pnpm -F ./web format"
  }
}
